config.yaml
# Any personal config copy (config.launchtest.yaml, config.local.yaml, ...) holds
# a real CVV and address. Ignore them all; the shipped template is the exception.
config.*.yaml
!config.example.yaml
__pycache__/
*.pyc
.venv/
venv/
.browser-profile/
.browser-profile-dynamic/
.DS_Store
*.log
.env
probe-report.json
dimension-probe.json
